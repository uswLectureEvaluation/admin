import { useState } from 'react';
// import { login } from '../api/login';

const Home = () => {
  const [A] = useState();

  return (
    <div>
      <h1>Home</h1>
      {A}
    </div>
  );
  //
};

export default Home;
